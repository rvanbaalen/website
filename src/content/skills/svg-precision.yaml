name: SVG Precision
description: Build SVGs from a strict JSON spec, then validate and render them
version: "1.1.0"
author: dkyazzentwatwa
invoke: "/svg-precision:svg-precision"
trigger: auto
category: skill
order: 9
