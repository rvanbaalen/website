---
import { getCollection } from "astro:content";
import BaseLayout from "../layouts/BaseLayout.astro";
import ArticleLedgerRow from "../components/ArticleLedgerRow.astro";
import ProjectRow from "../components/ProjectRow.astro";
import FadeIn from "../components/FadeIn.astro";
import KineticHero from "../components/KineticHero.astro";
import { personSchema, websiteSchema } from "../lib/seo";

const allArticles = await getCollection("articles", ({ data }) => !data.draft);
const articles = allArticles
  .sort((a, b) => b.data.date.getTime() - a.data.date.getTime())
  .slice(0, 4);

const allProjects = await getCollection("projects");
const featuredProjects = allProjects
  .filter((p) => p.data.featured && !p.data.archived)
  .sort((a, b) => a.data.order - b.data.order || a.data.name.localeCompare(b.data.name));
---

<BaseLayout
  title="Robin van Baalen — Founder & Software Engineer"
  description="Founder and software engineer. I build products, ship open source, and advise teams on software architecture and AI-assisted development."
  jsonLd={[personSchema(), websiteSchema()]}
>
  <KineticHero>
    Software should be simple. I build tools, advise teams, and ship code.
  </KineticHero>

  <div class="h-px bg-gradient-to-r from-edge to-transparent" />

  <!-- Selected Projects / Open Source -->
  <section class="py-12">
    <div class="mb-6 flex items-center justify-between font-sans text-xs font-medium text-dim">
      <h2 class="flex items-center gap-1.5 text-xs font-medium">
        <i class="ph ph-git-branch text-[15px]" aria-hidden="true" />
        Open source
      </h2>
      <a href="/open-source" class="flex items-center gap-1 text-accent transition-[color,gap] duration-150 hover:text-accent-bright hover:gap-2">
        View all <i class="ph ph-arrow-right text-[13px]" aria-hidden="true" />
      </a>
    </div>
    <div>
      {featuredProjects.map((project, i) => (
        <FadeIn delay={i * 80}>
          <ProjectRow
            name={project.data.name}
            description={project.data.description}
            tech={project.data.tech}
            url={project.data.url}
            repo={project.data.repo}
            hoverDetail={project.data.hover_detail}
          />
        </FadeIn>
      ))}
    </div>
  </section>

  <div class="h-px bg-gradient-to-r from-edge to-transparent" />

  <!-- Writing -->
  <section class="py-12">
    <div class="mb-6 flex items-center justify-between font-sans text-xs font-medium text-dim">
      <h2 class="flex items-center gap-1.5 text-xs font-medium">
        <i class="ph ph-notebook text-[15px]" aria-hidden="true" />
        Writing
      </h2>
      <a href="/writing" class="flex items-center gap-1 text-accent transition-[color,gap] duration-150 hover:text-accent-bright hover:gap-2">
        View all <i class="ph ph-arrow-right text-[13px]" aria-hidden="true" />
      </a>
    </div>
    {articles.length > 0 ? (
      <div>
        {articles.map((article, i) => (
          <FadeIn delay={i * 80}>
            <ArticleLedgerRow
              title={article.data.title}
              description={article.data.description}
              date={article.data.date}
              slug={article.id}
            />
          </FadeIn>
        ))}
      </div>
    ) : (
      <p class="px-4 text-muted">
        Nothing here yet. I only publish things I'd actually want to read.
      </p>
    )}
  </section>

  <!-- Contact -->
  <section id="contact" class="relative mt-14 rounded-md border border-edge bg-surface p-10 transition-colors duration-300">
    <div class="pointer-events-none absolute -inset-x-6 -inset-y-10 z-[-1] sm:-inset-x-20 [background:radial-gradient(ellipse_at_50%_40%,oklch(62%_0.04_260_/_calc(var(--gradient-intensity)_*_0.5)),transparent_40%)]" aria-hidden="true" />
    <h2 class="mb-2 flex items-center gap-2.5 text-xl font-normal text-primary">
      <i class="ph ph-coffee text-accent transition-colors duration-150" aria-hidden="true" />
      Get in touch
    </h2>
    <p class="mb-6 font-sans text-sm text-muted">
      Find me on one of these platforms.
    </p>
    <div class="grid gap-4 sm:grid-cols-2">
      <a href="https://github.com/rvanbaalen" target="_blank" rel="noopener noreferrer" class="group flex items-center gap-4 rounded-md border border-edge bg-canvas px-6 py-5 font-sans text-primary no-underline transition-colors duration-150 hover:border-accent-dim">
        <i class="ph ph-github-logo text-[32px] text-muted transition-colors duration-150 group-hover:text-accent" aria-hidden="true" />
        <div>
          <span class="block text-sm font-medium transition-colors duration-150 group-hover:text-accent">GitHub</span>
          <span class="block text-xs text-muted">@rvanbaalen</span>
        </div>
      </a>
      <a href="https://www.linkedin.com/in/robinvanbaalen/" target="_blank" rel="noopener noreferrer" class="group flex items-center gap-4 rounded-md border border-edge bg-canvas px-6 py-5 font-sans text-primary no-underline transition-colors duration-150 hover:border-accent-dim">
        <i class="ph ph-linkedin-logo text-[32px] text-muted transition-colors duration-150 group-hover:text-accent" aria-hidden="true" />
        <div>
          <span class="block text-sm font-medium transition-colors duration-150 group-hover:text-accent">LinkedIn</span>
          <span class="block text-xs text-muted">Robin van Baalen</span>
        </div>
      </a>
    </div>
  </section>
</BaseLayout>
